# Screenshots

These images show the native Pulse 0.1.4 interface with fictional sample tasks, projects and profile labels. They are UI illustrations, not evidence of live account connections, running agents or GitHub synchronization.

- `attention.jpg`: a pinned active task and a question needing attention.
- `background.jpg`: the background list with active, waiting and reviewed tasks.
- `task-details.jpg`: a sample question, its context and task controls.

Captured on macOS from an isolated copy of the app using the production panel views. The screenshot copy replaces live collection with an in-memory fixture and uses a separate data directory. No production UI styling was changed for these captures. The user's running Pulse instance and its settings were preserved.

If you replace these images, use sample data again, inspect every visible field, and update the captions to match the captured version. Keep original screenshot pixels; do not synthesize a product screen that claims features the app does not have.
